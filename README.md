# Ai-self-portrait
The goal of this project is to create an app that can visualize the 3d structure of ai. It should be a living repository that acts as a simple mirror. It will stay updated to current info about ai models. It will be a visual of a 3d tree-like structure that has connecting nodes that represent the parameters that it is trained on.
